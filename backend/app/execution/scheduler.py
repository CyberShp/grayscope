"""测试执行调度（占位，后续实现）。"""

from __future__ import annotations


def schedule_test_run(task_id: str, test_case_ids: list[int]) -> dict:
    """调度一批测试用例执行。占位返回空结果。"""
    return {"scheduled": 0, "message": "未实现"}
